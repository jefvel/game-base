
-cp src

# This will look for .aseprite files, and then export them,
# either as single pngs if they contain one frame, or a png + tilesheet
# when there are more than one frame.
--macro elke.buildutil.AsepriteConverter.exportTileSheets()